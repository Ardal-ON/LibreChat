# Use host-run semaa-agent (npm run dev) instead of the semaa-agent container.
services:
  semaa-agent:
    profiles:
      - docker-agent
  api:
    environment:
      - SEMAA_AGENT_BASE_URL=http://host.docker.internal:3333/v1
